import { ShowAuthenticateDirective } from './show-authenticate.directive';

describe('ShowAuthenticateDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAuthenticateDirective();
    expect(directive).toBeTruthy();
  });
});
